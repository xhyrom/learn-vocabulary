---
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const dictionary = await getCollection();

  return dictionary.map((entry) => {
    return {
      params: {
        id: entry.id,
      },
      props: {
        words: entry.data.words,
      },
    };
  });
}
---

yey
